const ANCHORS = document.querySelectorAll('a[href*="#"]');
const SITE_NAVIGATION = document.querySelector('.main-nav');
const SITE_NAVIGATION_TOGGLE = document.querySelector('.main-nav__toggle');
const DATA_FORM_INPUTS = document.querySelectorAll('input');

export {ANCHORS, SITE_NAVIGATION, SITE_NAVIGATION_TOGGLE, DATA_FORM_INPUTS};
